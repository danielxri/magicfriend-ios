import Foundation

struct Config {
    // CHANGE THIS to your Production URL (e.g., https://api.magicfriend.app)
    // For local testing on Simulator, use http://localhost:5001
    // For local testing on Device, use your Mac's LAN IP (e.g., http://192.168.1.5:5001)
    static let baseURL = "http://127.0.0.1:5001"
}
